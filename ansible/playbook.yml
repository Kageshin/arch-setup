- name: Provision Arch Linux
  hosts: localhost
  become: true

  tasks:
    - name: Install packages
      pacman:
        name: "{{ item }}"
        state: present
      loop: "{{ lookup('file', '../pkglist.txt').splitlines() }}"
